---
- name: Docker
  ansible.builtin.import_role: { name: docker }
  vars: { docker_users: ["{{ jellyfin_owner }}"] }

- name: Get directories info
  ansible.builtin.file: { path: "{{ item.path }}" }
  check_mode: true
  failed_when: false
  register: jellyfin_dirs       # jellyfin_dirs.results[ix].path
  loop_control: { label: "{{ item.user }}:{{ item.path }}" }
  loop:
    - { path: "{{ jellyfin_conf_dir }}", user: "{{ jellyfin_owner }}" }
    - { path: "{{ jellyfin_data_dir }}", user: "{{ jellyfin_user }}" }
  become: true
  become_user: "{{ item.user }}"

- name: Compose
  become: true
  become_user: "{{ jellyfin_owner }}"
  vars:
    compose_dir: "{{ jellyfin_compose_dir | default(bookshelf_const_compose_dir + '/jellyfin', true) }}"
    conf_dir_abs: "{{ jellyfin_dirs.results[0].path }}"
    data_dir_abs: "{{ jellyfin_dirs.results[1].path }}"
    service_state: "{{ jellyfin_enabled | ternary('present', 'absent') }}"
    the_user: "{{ getent_passwd[jellyfin_user] }}"
    user_uid: "{{ the_user.1 }}"
    user_gid: "{{ the_user.2 }}"
  block:
    - name: Create data directory
      ansible.builtin.file:
        path: "{{ data_dir_abs }}"
        state: directory
        mode: "{{ omit }}"
      become: true
      become_user: "{{ jellyfin_user }}"

    - name: Create compose directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: '0700'
      loop: ["{{ compose_dir }}", "{{ conf_dir_abs }}/config"]

    - name: Create compose files
      ansible.builtin.template:
        src: "{{ item }}.j2"
        dest: "{{ compose_dir }}/{{ item }}"
        mode: '0600'
      loop:
        - serve.json    # <- Must be first
        - compose.yaml
        - main.env
        - secret.env
      register: jellyfin_compose_files_result

    - name: Ensure service user directories ownership
      ansible.builtin.file:
        path: "{{ item }}"
        owner: "{{ user_uid }}"
        group: "{{ user_gid }}"
      loop: ["{{ data_dir_abs }}", "{{ conf_dir_abs }}/config"]
      become: true
      become_user: root

    - name: Apply service state
      community.docker.docker_compose_v2:
        project_src: "{{ compose_dir }}"
        state: "{{ service_state }}"
        remove_orphans: true
      register: jellyfin_service_state

    - name: Restart service
      community.docker.docker_compose_v2:
        project_src: "{{ compose_dir }}"
        state: restarted
        remove_orphans: true
      when:
        - jellyfin_enabled and not jellyfin_service_state.changed
        - jellyfin_tailscaled and jellyfin_compose_files_result.results[0].changed

    - name: Create note
      ansible.builtin.import_role: { name: bookshelf-note }
      # noqa: var-naming[no-role-prefix]
      vars: { src: notes.md.j2, dest: jellyfin-service.md, present: "{{ jellyfin_enabled }}" }

- name: Mark done
  ansible.builtin.set_fact:
    jellyfin_done: true
