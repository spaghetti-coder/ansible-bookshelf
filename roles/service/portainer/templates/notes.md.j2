## Portainer
{% if portainer_tailscaled %}

* Disable tailscale machine expiry:
  1. Go to https://tailscale.com/admin/machines
  2. `{{ portainer_ts_hostname }} *** > Disable key expiry`
{% endif %}
