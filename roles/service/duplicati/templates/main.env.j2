# STATIC
# ======
PUID=0  # <- In container root user to avoid permission issues while backing up
PGID=0

# CONFIG
# ======
TZ={{ duplicati_tz }}

# TAILSCALE
# =========
{{ lookup('template', './../../tailscale/partial/sidecar-main.env.j2', template_vars=dict(
  ts_hostname = duplicati_ts_hostname
)) }}

# EXTRA
# =====
{{ duplicati_extra_env }}
