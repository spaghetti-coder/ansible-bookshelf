{{ lookup('template', './../../tailscale/partial/sidecar-serve.json.j2', template_vars=dict(
  ts_web_proxy = 'https://127.0.0.1:' + (caddy_web_ui_port_int | string),
  ts_funnelled = blinko_ts_funnelled
)) }}
