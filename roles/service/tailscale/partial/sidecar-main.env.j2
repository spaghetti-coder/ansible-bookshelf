{#

# USAGE:
# -----
{{ lookup('template', playbook_dir + '/roles/service/tailscale/partial/tailscale-main.env.j2', template_vars=dict(
  ts_hostname = <TAILSCALE_INSTANCE_HOSTNAME>
)) }}

#}
TS_STATE_DIR=/var/lib/tailscale
TS_SERVE_CONFIG=/config/serve.json
TS_USERSPACE=false
TS_HOSTNAME={{ ts_hostname | default('', true) }}
# Healthcheck configuration
TS_ENABLE_HEALTH_CHECK=true         # <- Enable "/healthz" endpoint
TS_LOCAL_ADDR_PORT=127.0.0.1:39148  # <- {ADDR}:{PORT} for the healthz endpoint
# TS_ACCEPT_DNS=false   # <- (OPTIONAL) Set to true to use MagicDNS
