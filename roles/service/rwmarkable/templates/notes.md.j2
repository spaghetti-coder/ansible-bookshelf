## rwMarkable
{% if rwmarkable_tailscaled %}

* Disable tailscale machine expiry:
  1. Go to https://tailscale.com/admin/machines
  2. `{{ rwmarkable_tailscale_ts_hostname }} *** > Disable key expiry`
{% endif %}
