## Mealie
{% if mealie_tailscaled %}

* Disable tailscale machine expiry:
  1. Go to https://tailscale.com/admin/machines
  2. `{{ mealie_tailscale_ts_hostname }} *** > Disable key expiry`
{% endif %}
