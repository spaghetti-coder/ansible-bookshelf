---
- name: Docker
  ansible.builtin.import_role: { name: docker }
  vars: { docker_users: ["{{ vikunja_owner }}"] }

- name: Compose
  become: true
  become_user: "{{ vikunja_owner }}"
  vars:
    compose_dir: "{{ vikunja_compose_dir | default(bookshelf_const_compose_dir + '/vikunja', true) }}"
    conf_dir: "{{ vikunja_conf_dir }}"
    service_state: "{{ vikunja_enabled | ternary('present', 'absent') }}"
    the_user: "{{ getent_passwd[vikunja_owner] }}"
    user_uid: "{{ the_user.1 }}"
    user_gid: "{{ the_user.2 }}"
  block:
    - name: Create directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: '0700'
      loop:
        - "{{ compose_dir }}"
        - "{{ conf_dir }}"

    - name: Create .env files
      ansible.builtin.template:
        src: "{{ item }}.env.j2"
        dest: "{{ compose_dir }}/{{ item }}.env"
        mode: '0600'
      loop:
        - main
        - secret

    - name: Create extra.env
      ansible.builtin.copy:
        content: |
          {{ vikunja_extra_env | default('', true) }}
        dest: "{{ compose_dir }}/extra.env"
        mode: '0600'

    - name: Create serve.json
      ansible.builtin.template:
        src: "serve.json.j2"
        dest: "{{ compose_dir }}/serve.json"
        mode: '0600'
      when: vikunja_tailscaled

    - name: Compose apply
      ansible.builtin.import_role: { name: bookshelf-compose-apply }
      # noqa: var-naming[no-role-prefix]
      vars: { template: compose.yaml.j2, dest_dir: "{{ compose_dir }}", state: "{{ service_state }}" }

    - name: Create note
      ansible.builtin.import_role: { name: bookshelf-note }
      # noqa: var-naming[no-role-prefix]
      vars: { src: notes.md.j2, dest: vikunja-service.md }
      when: vikunja_tailscaled

- name: Mark done
  ansible.builtin.set_fact:
    vikunja_done: true
