# STATIC
# ======
WS_FALLBACK=true

# CONFIG
# ======
PUID={{ user_uid }}
PGID={{ user_gid }}
TZ={{ pairdrop_tz }}

# TAILSCALE
# =========
{{ lookup('template', './../../tailscale/partial/sidecar-main.env.j2', template_vars=dict(
  ts_hostname = pairdrop_ts_hostname
)) }}

# EXTRA
# =====
{{ pairdrop_extra_env }}
