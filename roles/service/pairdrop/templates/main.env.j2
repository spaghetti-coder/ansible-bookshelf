# STATIC
# ======
WS_FALLBACK=true
# RATE_LIMIT=false #optional
# RTC_CONFIG= #optional
# DEBUG_MODE=false #optional

# CONFIG
# ======
PUID={{ user_uid }}
PGID={{ user_gid }}
TZ={{ pairdrop_tz }}
{% if pairdrop_tailscaled %}

# TAILSCALE
# =========
TS_STATE_DIR=/var/lib/tailscale
TS_SERVE_CONFIG=/config/serve.json
TS_USERSPACE=false
{% if pairdrop_tailscale_ts_hostname %}
TS_HOSTNAME={{ pairdrop_tailscale_ts_hostname }}
{% endif %}
{% endif %}{# pairdrop_tailscaled #}
