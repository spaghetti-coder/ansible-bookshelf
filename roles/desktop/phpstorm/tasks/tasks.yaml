---
- name: Snapd
  ansible.builtin.include_role: {'name': 'snapd'}

- name: Install
  community.general.snap:
    name: phpstorm
    classic: true
  become: true

- name: Mark install done
  ansible.builtin.set_fact:
    phpstorm_install_done: true
