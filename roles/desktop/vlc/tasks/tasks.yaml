---
- name: Snapd
  ansible.builtin.include_role: {'name': 'snapd'}

- name: Install
  community.general.snap:
    name: vlc
  become: true

- name: Mark install done
  ansible.builtin.set_fact:
    vlc_done: true
