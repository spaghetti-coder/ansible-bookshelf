---
- name: Epel-release
  ansible.builtin.include_role:
    name: epel-release

- name: Install
  ansible.builtin.package:
    name: htop
  become: true

- name: Mark done
  ansible.builtin.set_fact:
    htop_done: true
