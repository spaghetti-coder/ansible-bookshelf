---
# DEMO:
# ====
# - name: Create note
#   ansible.builtin.import_role: { name: bookshelf-note }
#   # noqa: var-naming[no-role-prefix]
#   vars:
#     src: notes.md.j2      # <- Source file in the parent templates/
#     # src: notes.md       # <- Source file in the parent files/
#     dest: qbittorrent.md  # <- Desination file name under bookshelf_note_home directory
#     present: true         # <- (Optional) Defaults to true

- name: Load state tasks
  ansible.builtin.include_tasks:
    file: tasks-{{ state }}.yaml
  vars:
    state: "{{ present | ternary('present', 'absent') }}"
