---
- name: Node
  ansible.builtin.import_role: { name: nodejs }

- name: Install
  community.general.npm:
    name: '@opengsd/get-shit-done-redux'
    global: true
  become: true

- name: Mark dones
  ansible.builtin.set_fact:
    get_shit_done_done: true
